#pragma once
#include "stdafx.h"

/*class Spell
{
public:
	Spell(sf::Vector2f size)
	{
		spell.setSize(size);
		spell.setFillColor(sf::Color::Cyan);
	}

	void fire(int speed)
	{
		spell.move(speed, 0);
	}

	int getRight()
	{
		return spell.getPosition().x + spell.getSize().x;
	}

	int getLeft()
	{
		return spell.getPosition().x;
	}

	int getTop()
	{
		return spell.getPosition().y;
	}

	int getBottom()
	{
		return spell.getPosition().y + spell.getSize().y;
	}

	void draw();
	
	Spell();

private:
	
};*/